{
  "name": "walless-database",
  "version": "0.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "migrate": "knex migrate:latest --knexfile \"db.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Touko Hallasmaa",
  "devDependencies": {
    "eslint": "^4.0.0",
    "eslint-config-google": "^0.9.1",
    "knex-seed-file": "^0.4.1"
  },
  "dependencies": {
    "knex": "^0.13.0",
    "pg": "^7.3.0"
  },
  "engines": {
    "node": ">=6.0.0"
  }
}
